title: Just Interesting Things
description: Daily journal.

# This is a GitHub *project* site (repo name "bored"), so it is served from a
# subpath: https://weird-kid.github.io/bored/
# These settings make internal links resolve correctly.
url: "https://weird-kid.github.io"
baseurl: "/bored"

markdown: kramdown
